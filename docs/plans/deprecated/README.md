# Deprecated Plan Handoffs

This folder contains completed handoff documents that have been superseded by
the sharded architecture docs in `docs/architecture/`.

They are retained only as tombstones and historical references. New work should
not use these files as authoritative source. Use `docs/architecture/` plus
`docs/plans/sprint-roadmap.md` instead.

