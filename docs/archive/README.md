# Documentation Archive

This directory keeps superseded product evidence that is no longer the current
baseline but may still be useful for audit comparison.

Current architecture and promotion decisions should use the live docs under
`docs/architecture/`, `docs/plans/`, `docs/dogfood/`, and `docs/reports/`.
Archived files are historical inputs only.
