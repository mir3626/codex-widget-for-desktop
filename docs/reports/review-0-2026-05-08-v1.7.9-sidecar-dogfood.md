# /vibe-review Dogfood Verification - v1.7.9 Sidecar - 2026-05-08

## Inputs loaded

- Synced downstream `codex-widget-for-desktop` to vibe-doctor `v1.7.9` with `npm run vibe:sync -- --ref v1.7.9 --force`.
- Sync backup: `.vibe/sync-backup/2026-05-07T17-38-19-898Z`.
- Installed harness version: `.vibe/config.json` records `harnessVersion: "1.7.9"` and `harnessVersionInstalled: "1.7.9"`. The downstream upstream ref was advanced to `^v1.7.9`.
- `package.json` now has `vibe:sidecar-run`.
- Conflict preservation: `docs/context/orchestration.md` and `docs/orchestration/providers.md` keep this downstream's Codex Orchestrator/Planner/Generator/Evaluator contract while accepting the new sidecar guidance.
- Ran `node .vibe/harness/scripts/vibe-review-inputs.mjs --install`; helper reported `openHarnessGapCount: 0`, `wiringDriftFindings: []`, and `issues: []`.
- Helper still reports `gap-sidecar-review-coverage` in `uncoveredHarnessGaps[]` and `deadlineHarnessGaps[]` because the ledger row is `under-review | partial | +3 sprints`.

## Sidecar dogfood runs

| Sprint id | Command | Status | Notes |
|---|---|---|---|
| `dogfood-sidecar-v179` | `npm run vibe:sidecar-run -- diff-reviewer --sprint-id dogfood-sidecar-v179` | `error` | Auto-selected `provider: codex`, but Windows Node spawn failed with `spawnSync codex ENOENT`. |
| `dogfood-sidecar-v179-codex` | `npm run vibe:sidecar-run -- diff-reviewer --sprint-id dogfood-sidecar-v179-codex --provider codex` | `error` | Explicit provider reproduced the same Windows shim failure. |
| `dogfood-sidecar-v179-codex-fixed` through `dogfood-sidecar-v179-codex-final3` | explicit Codex provider | `advisory` / `fail` | Useful findings exposed wrapper gaps: untracked diff omission, inputHash validation, wrapper-owned coverage, semantic status/finding validation, `--cwd` path resolution, artifact-root escape, tracked secret redaction, and Claude adapter tool boundary. |
| `dogfood-sidecar-v179-codex-final6` | `npm run vibe:sidecar-run -- diff-reviewer --sprint-id dogfood-sidecar-v179-codex-final6 --provider codex --timeout-ms 180000` | `advisory` | Latest artifact is valid JSON. Its only finding was a low-severity stale artifact-reference note in this report/handoff, accepted and corrected after the run. |

Final artifact:

- Path: `.vibe/sidecars/artifacts/dogfood-sidecar-v179-codex-final6/diff-reviewer.json`
- `sidecar`: `diff-reviewer`
- `provider`: `codex`
- `model`: `gpt-5.5`
- `effort`: `high`
- `status`: `advisory`
- `inputHash`: `sha256:eb6701058e4e17c974107bf587dfdb5ec151f59e784b758509034a5a7eb06942`
- `coverage`: `{ inputFilesSeen: 23, diffBytesSeen: 21144, truncated: true }`
- Latest finding: low-severity documentation consistency note because this report/handoff still referenced `final4`; corrected after the run.
- Limitation: new untracked files are represented by omitted-content placeholders by default, so their internals were not sent to the external provider.

## Durable-state mutation check

Sidecar execution itself did not mutate durable state:

- `.vibe/agent/handoff.md`: hash stayed `c5a9988505d167608335892cde921f6ef68131db`
- `.vibe/agent/session-log.md`: hash stayed `b4ab71aa476826e71f8d4f6ead425f1d5e228cc4`
- `.vibe/agent/sprint-status.json`: hash stayed `238b9b9ffa78e5d54a1994e2730bb6c90a8c4ffb`
- `git diff --name-only -- .vibe/agent/handoff.md .vibe/agent/session-log.md .vibe/agent/sprint-status.json src src-tauri docs/reports` was empty immediately after sidecar runs.

The later `/vibe-review` report and maintain-context updates are Orchestrator-owned durable writes, not sidecar writes.

## Findings (severity desc)

### review-sidecar-windows-codex-provider-spawn

```yaml
- id: review-sidecar-windows-codex-provider-spawn
  severity: friction
  priority: P1
  proposal: Keep the Windows Codex sidecar adapter fix: resolve the npm-installed Codex JS entrypoint and run it through `node` instead of `spawnSync("codex")`, which cannot execute the Windows shim in this context.
  estimated_loc: 35
  proposed_sprint: accepted-in-dogfood
```

- Vanilla v1.7.9 failed both auto and explicit Codex provider runs on Windows with `spawnSync codex ENOENT`.
- The local dogfood patch made the same command produce real `codex` artifacts.
- This should be upstreamed because the downstream Orchestrator default provider is Codex on Windows.

### review-sidecar-sealed-packet-hardening

```yaml
- id: review-sidecar-sealed-packet-hardening
  severity: structural
  priority: P1
  proposal: Keep the sealed-packet hardening accepted during dogfood: recompute `--input-file` hashes, keep wrapper-owned coverage, reject contradictory status/finding output, constrain artifact roots, resolve relative inputs against `--cwd`, and avoid sending sensitive tracked/untracked file contents.
  estimated_loc: 120
  proposed_sprint: accepted-in-dogfood
```

- The sidecar produced useful findings that Orchestrator self-QA had not caught.
- Accepted fixes are covered by `node --import tsx --test .vibe/harness/test/sidecar.test.ts`.
- Default untracked file handling is intentionally conservative: paths are visible, contents are omitted unless a future explicit opt-in is designed.

### review-sidecar-coverage-remains-partial

```yaml
- id: review-sidecar-coverage-remains-partial
  severity: structural
  priority: P2
  proposal: Move `gap-sidecar-review-coverage` toward covered, but keep it `partial` until at least a few more dogfood runs prove useful signal with tolerable truncation/omitted-content limits and until any planned `/vibe-review` rollup remains report-only.
  estimated_loc: 0
  proposed_sprint: backlog
```

- The foundation is now demonstrably useful: it found real provider, security, and contract issues and produced a final valid artifact with only a low documentation-reference note that was accepted and corrected.
- It is not fully covered yet because it is manual-only, advisory-only, ignores artifacts by design, and omits untracked contents by default for safety.
- Recommendation: `gap-sidecar-review-coverage` should **move toward covered but remain partial** for now, not be closed.

## Verification

Passed:

- `npm run vibe:typecheck`
- `node .vibe/harness/scripts/vibe-preflight.mjs --bootstrap`
- `npm run vibe:gen-schemas -- --check`
- `node --import tsx --test .vibe/harness/test/sidecar.test.ts`

Known non-blocking test note:

- `npm run vibe:self-test -- --test-name-pattern sidecar` invoked the full harness suite despite the name pattern and failed one unrelated `schemas.test.ts` production payload check because the current downstream `sprint-status` timestamps are not strict Zod datetimes. The isolated sidecar suite passed.

## Upstream follow-up

- Upstream should take the downstream v1.7.9 dogfood fixes before treating the sidecar foundation as Windows/Codex-ready.
- No product-code changes were required.
