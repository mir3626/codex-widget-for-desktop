# /vibe-review Dogfood Verification - v1.7.8 - 2026-05-07

## Inputs loaded

- Synced downstream `codex-widget-for-desktop` from vibe-doctor `1.7.7` to `1.7.8` with `npm run vibe:sync -- --force --ref v1.7.8` after dry-run/normal sync showed two known downstream doc conflicts.
- Installed harness version: `.vibe/config.json` now records `harnessVersion: "1.7.8"`, `harnessVersionInstalled: "1.7.8"`, and upstream ref `^v1.7.8`.
- Sync backup: `.vibe/sync-backup/2026-05-07T14-12-56-291Z`.
- Conflict resolution: preserved downstream Codex Orchestrator/Planner/Generator/Evaluator contracts in `docs/context/orchestration.md` and `docs/orchestration/providers.md`; kept upstream v1.7.8 semantic acceptance guidance.
- Ran `node .vibe/harness/scripts/vibe-review-inputs.mjs --install`; stderr was empty.
- Helper summary: `openHarnessGapCount: 0`; `uncoveredHarnessGaps`: `gap-rule-only-in-md`, `gap-harness-bloat-self-expansion`, `gap-pass-only-product-identity`, `gap-context-overhead-policy`; `deadlineHarnessGaps`: same four rows; `pendingRestorations: 0`; `productFetcherPaths: []`; `wiringDriftFindings: []`; `pendingRiskRollups: []`; opt-in `issues: []`.
- Ran `npm run vibe:context-audit` as report-only evidence: `contextBytes: 239117`, `references: 416`, `findings: 355`, `missing: 48`, `ambiguous: 307`.
- Verification commands passed: `npm run vibe:typecheck`, `node .vibe/harness/scripts/vibe-preflight.mjs --bootstrap`, and `node --import tsx --test .vibe/harness/test/vibe-review-inputs.test.ts .vibe/harness/test/review-regression.test.ts .vibe/harness/test/decisions.test.ts` (26 tests).

## Regression verification

| Regression | Status | Evidence |
|---|---|---|
| Pipe-heavy harness-gap ledger rows | PASS | Covered rows containing `bundle.policy=automatic|custom|off`, `\|\| true`, and `base === sprintId \|\| base.startsWith(...)` no longer appear in `uncoveredHarnessGaps[]`. Only actual `under-review` / `partial` rows remain. |
| Findings heading contract | PASS | v1.7.8 parser accepts `## Findings (...)`; upstream regression test `loadPriorReviewIssues accepts the documented parenthesized findings heading` passed. This report intentionally uses `## Findings (severity desc)` below. |
| Legacy decisions JSONL warning | PASS | Existing minimal bootstrap record in `.vibe/agent/project-decisions.jsonl` no longer emits `[decisions] invalid record skipped`; malformed-record tests still show file/line/schema guidance. |
| Context-audit policy remains report-only | PASS | Context audit is summarized as counters only. No prompt/capsule/context reduction is recommended from size/noise numbers. |
| Semantic acceptance evidence | PASS WITH RESIDUAL | v1.7.8 adds semantic agent-context evidence guidance. Downstream Vision Context remains dogfood-ready but not semantically accepted until a transcript, screen-share note, task-quality artifact, or before/after outcome exists. |

## Findings (severity desc)

### review-context-overhead-remains-report-only

```yaml
- id: review-context-overhead-remains-report-only
  severity: structural
  priority: P2
  proposal: Keep context-audit output report-only and use it as observability, not as a prompt/capsule reduction trigger, until concrete dogfood evidence proves safe fail-closed behavior.
  estimated_loc: 0
  proposed_sprint: backlog
```

- `gap-context-overhead-policy` remains `under-review | partial | +3 sprints`, which is expected after v1.7.8.
- Current counters are useful for review context but not independently actionable: `contextBytes: 239117`, `references: 416`, `missing: 48`, `ambiguous: 307`.
- No upstream regression was found here; the v1.7.8 workflow now asks for compact counters and preserves the report-only policy.

### review-semantic-acceptance-evidence-still-needed

```yaml
- id: review-semantic-acceptance-evidence-still-needed
  severity: structural
  priority: P2
  proposal: Treat the downstream Vision Context MVP as dogfood-ready rather than semantically accepted until it has a real transcript, screen-share note, task-quality artifact, or before/after task outcome.
  estimated_loc: 0
  proposed_sprint: backlog
```

- `gap-pass-only-product-identity` remains `under-review | partial | +2 sprints`, now with v1.7.8 semantic agent-context guidance.
- The downstream MVP has shape coverage for TaskCapsule markdown, localImage delivery, raw media deletion, resolver behavior, ASR mock/correction, and destructive clarification.
- That coverage proves the interface shape, not that the capsule improves an Agent task. This is a downstream dogfood evidence gap, not a product-code failure.

## Suggested next-sprint scope

1. No immediate upstream parser follow-up is required for the three specific v1.7.8 fixes verified here.
2. Collect one Vision Context dogfood transcript or task-quality artifact before calling the semantic feature accepted.
3. Keep context-audit as report-only observability; do not gate or reduce context routing from current noise counters alone.

## Links

- `.vibe/config.json`
- `.vibe/harness/src/lib/review.ts`
- `.vibe/harness/src/lib/decisions.ts`
- `.vibe/harness/test/vibe-review-inputs.test.ts`
- `.vibe/harness/test/review-regression.test.ts`
- `.vibe/harness/test/decisions.test.ts`
- `docs/context/harness-gaps.md`
- `docs/context/orchestration.md`
- `docs/orchestration/providers.md`
