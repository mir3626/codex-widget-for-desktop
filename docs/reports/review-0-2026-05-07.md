# /vibe-review 0 - 2026-05-07

## Inputs loaded

- Ran `node .vibe/harness/scripts/vibe-review-inputs.mjs --install`; `detectOptInGaps()` ran first through the helper and returned no opt-in issues.
- Read `.vibe/agent/handoff.md`, recent `.vibe/agent/session-log.md`, `.vibe/agent/sprint-status.json`, `.vibe/agent/project-decisions.jsonl`, `.vibe/config.json`, and `docs/context/harness-gaps.md`.
- Read latest `git log --oneline -20`; no prior `docs/reports/review-*.md` report exists, so the helper used `gitLogMode: recent`.
- Checked `.vibe/archive/rules-deleted-*.md` and `.vibe/audit/iter-*/rules-deleted.md`; no files were present.
- Helper state: `passedSprintCount: 0`, `openHarnessGapCount: 0`, `pendingRestorations: 0`, `productFetcherPaths: []`, `wiringDriftFindings: []`, `pendingRiskRollups: []`.
- Ledger signals still require findings: `uncoveredHarnessGaps` included `gap-rule-only-in-md`, `gap-harness-bloat-self-expansion`, `gap-pass-only-product-identity`, and `gap-context-overhead-policy`; `deadlineHarnessGaps` included the same active deadline rows. The helper also overreported several covered rows because of table parsing drift, covered below.
- Ran `npm run vibe:context-audit` as report-only supporting evidence for `gap-context-overhead-policy`; it reported `contextBytes: 237582`, `references: 416`, `findings: 355`, `missing: 48`, and `ambiguous: 307`.

## Findings

### review-gap-ledger-pipe-parser

```yaml
- id: review-gap-ledger-pipe-parser
  severity: friction
  priority: P1
  proposal: Make the harness-gap ledger parser escaped-pipe and code-span aware, or move the ledger to a structured source before parsing. Add regressions using the current rows for bundle.policy choices, shell `||`, and archive prompt matching so covered rows are not surfaced as uncovered review evidence.
  estimated_loc: 80
  proposed_sprint: backlog
```

- `vibe-review-inputs` reported `gap-opt-in-visibility`, `gap-windows-hook-command-portability`, and `gap-archive-prompts-regex` as uncovered even though the source ledger rows in `docs/context/harness-gaps.md` mark them `covered | covered`.
- The affected rows contain pipe-like content inside code spans or escaped Markdown, such as `automatic|custom|off`, `\|\| true`, and `base === sprintId \|\| base.startsWith(...)`.
- `.vibe/harness/src/lib/review.ts` currently parses ledger cells with a direct `.split('|')`, so escaped or code-span pipes shift later columns into `status`, `scriptGate`, and `migrationDeadline`.
- `.vibe/harness/test/vibe-review-inputs.test.ts` covers simple six-column rows but does not include escaped-pipe or code-span-pipe regression rows.

### review-findings-heading-contract

```yaml
- id: review-findings-heading-contract
  severity: friction
  priority: P1
  proposal: Align `/vibe-review` report-heading instructions with the prior-review parser, or relax the parser to accept `## Findings (severity desc)`. Otherwise future reports written exactly from the skill text can be invisible to regression checks.
  estimated_loc: 20
  proposed_sprint: backlog
```

- `.claude/skills/vibe-review/SKILL.md` documents the report shape as `## Findings (severity desc)`.
- `.vibe/harness/src/lib/review.ts` extracts prior findings with `^## Findings\s*$`, which only matches an exact `## Findings` heading.
- This report uses exact `## Findings` so its YAML blocks remain parseable by existing harness code, but the documented workflow still invites future drift.

### review-context-audit-observability-dogfood

```yaml
- id: review-context-audit-observability-dogfood
  severity: structural
  priority: P2
  proposal: Keep `vibe-context-audit` report-only, but promote a concise review summary and dependency-strength cleanup sprint before any prompt/capsule reduction work. Do not reduce context routing until warning-only observability has dogfood evidence and fail-closed behavior is defined.
  estimated_loc: 120
  proposed_sprint: backlog
```

- `docs/context/harness-gaps.md` keeps `gap-context-overhead-policy` at `under-review | partial | +3 sprints` and explicitly blocks capsule/router prompt reduction until observability and fail-closed safeguards are dogfooded.
- The current review had to read large durable context surfaces after a long Vision Context iteration; the overhead is real, but the durable markers also carried important corrections and completion evidence.
- `npm run vibe:context-audit` is useful but noisy today: `355` findings across `416` references, with `48` missing and `307` ambiguous. That is not yet a reliable gate or a safe basis for context reduction.

### review-semantic-acceptance-evidence

```yaml
- id: review-semantic-acceptance-evidence
  severity: structural
  priority: P2
  proposal: Extend the pass-only product identity rule to semantic agent-context features by requiring one dogfood transcript, screen-share demonstration, or task-quality acceptance artifact in addition to type/build/smoke checks.
  estimated_loc: 60
  proposed_sprint: backlog
```

- `gap-pass-only-product-identity` remains `under-review | partial | +2 sprints`; its core lesson is that machine acceptance checks can miss whether the shipped slice actually feels like the intended product.
- The Vision Context MVP has strong shape coverage: TaskCapsule markdown, raw media deletion, resolver cases, mock ASR, lexicon correction, destructive clarification, and fake app-server `localImage` delivery.
- The current durable state says Vision Context is ready for dogfooding, not yet dogfood-accepted. For a semantic context feature, a real or scripted user-task transcript is the missing acceptance signal, not another typecheck.

### review-decisions-jsonl-schema-warning

```yaml
- id: review-decisions-jsonl-schema-warning
  severity: polish
  priority: P3
  proposal: Normalize the existing project-decisions JSONL bootstrap record or make the review helper warning include file, line, and expected schema. The current warning is harmless but repeats every review and reduces confidence in the input set.
  estimated_loc: 25
  proposed_sprint: backlog
```

- `node .vibe/harness/scripts/vibe-review-inputs.mjs --install` prints `[decisions] invalid record skipped` for the only record in `.vibe/agent/project-decisions.jsonl`.
- The file is valid JSONL, but the bootstrap record has only `timestamp`, `tag`, `decision`, and `reason`, while the loader expects the richer decision shape used by later harness state.
- Because this is a review-input warning, it should be either repaired once or made self-diagnosing enough that future reviewers do not have to inspect the file manually.

## Suggested next-sprint scope

1. Fix the harness-gap ledger parser and add regression tests using the current escaped-pipe rows. This is the highest-leverage review reliability fix.
2. Align the documented `/vibe-review` Findings heading with the prior-review parser, or relax the parser and test both accepted headings.
3. Add a compact `/vibe-review` context-audit summary path that records only counts and top actionable classes, then classify or suppress known optional missing references.
4. Add semantic acceptance evidence guidance for agent-context, multimodal, and other non-visual features whose value is not captured by shape-only smokes.
5. Normalize `.vibe/agent/project-decisions.jsonl` or improve the warning with exact schema guidance.

## Links

- `docs/context/harness-gaps.md`
- `.vibe/harness/src/lib/review.ts`
- `.vibe/harness/test/vibe-review-inputs.test.ts`
- `.vibe/agent/project-decisions.jsonl`
- `.vibe/agent/session-log.md`
- `.vibe/agent/handoff.md`
